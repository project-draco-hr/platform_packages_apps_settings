{
  try {
    mCurConfig.updateFrom(ActivityManagerNative.getDefault().getConfiguration());
  }
 catch (  RemoteException e) {
  }
  mToggleLargeTextCheckBox.setChecked(Float.compare(mCurConfig.fontScale,LARGE_FONT_SCALE) == 0);
}
