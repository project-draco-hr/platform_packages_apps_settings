{
  super.onResume();
  if (!"com.android.certinstaller".equals(getCallingPackage()))   finish();
  if (!isKeyStoreLocked()) {
    install();
    finish();
  }
 else   if (!mUnlocking) {
    mUnlocking=true;
    Credentials.getInstance().unlock(this);
  }
 else {
    finish();
  }
}
