{
  final String intentAction=intent.getAction();
  if (Intent.ACTION_USER_INITIALIZE.equals(intentAction) && UserHandle.USER_OWNER != UserHandle.myUserId()) {
    disableCryptKeeperComponent(context);
  }
}
