{
  boolean isEnable=false;
  if (mMobileDataEnabled != null) {
    isEnable=mMobileDataEnabled.booleanValue();
    mMobileDataEnabled=null;
  }
 else {
    isEnable=mTelephonyManager.getDataEnabled() && (subId == mSubscriptionManager.getDefaultDataSubId());
  }
  return isEnable;
}
