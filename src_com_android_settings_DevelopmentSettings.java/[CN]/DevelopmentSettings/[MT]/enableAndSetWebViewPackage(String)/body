{
  getActivity().getPackageManager().setApplicationEnabledSetting(packageName,PackageManager.COMPONENT_ENABLED_STATE_ENABLED,0);
  mWebViewProvider.setValue(packageName);
  writeWebViewProviderOptions(packageName);
}
