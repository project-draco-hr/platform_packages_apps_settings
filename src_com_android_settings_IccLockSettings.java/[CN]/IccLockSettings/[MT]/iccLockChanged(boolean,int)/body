{
  if (success) {
    mPinToggle.setChecked(mToState);
  }
 else {
    Toast.makeText(getActivity(),getPinPasswordErrorMessage(attemptsRemaining),Toast.LENGTH_LONG).show();
  }
  mPinToggle.setEnabled(true);
  resetDialogState();
}
