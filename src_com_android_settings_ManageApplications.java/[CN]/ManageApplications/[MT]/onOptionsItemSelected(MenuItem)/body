{
  int menuId=item.getItemId();
  if ((menuId == SORT_ORDER_ALPHA) || (menuId == SORT_ORDER_SIZE)) {
    sendMessageToHandler(REORDER_LIST,menuId);
  }
 else   if (menuId == FILTER_OPTIONS) {
    if (mAlertDlgBuilder == null) {
      mAlertDlgBuilder=new AlertDialog.Builder(this).setTitle(R.string.filter_dlg_title).setNeutralButton(R.string.cancel,this).setSingleChoiceItems(new CharSequence[]{getText(R.string.filter_apps_all),getText(R.string.filter_apps_running),getText(R.string.filter_apps_third_party)},-1,this);
    }
    mAlertDlgBuilder.show();
  }
  return true;
}
