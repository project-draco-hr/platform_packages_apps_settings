{
  Resources res=getActivity().getResources();
  return new ChooseLockSettingsHelper(getActivity(),this).launchConfirmationActivity(request,null,res.getText(R.string.reset_network_gesture_explanation));
}
