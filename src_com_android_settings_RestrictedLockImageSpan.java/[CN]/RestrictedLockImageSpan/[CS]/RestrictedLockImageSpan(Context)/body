{
  super((Drawable)null);
  mContext=context;
  mExtraPadding=mContext.getResources().getDimensionPixelSize(R.dimen.restricted_lock_icon_padding);
  mRestrictedPadlock=RestrictedLockUtils.getRestrictedPadlock(mContext);
}
