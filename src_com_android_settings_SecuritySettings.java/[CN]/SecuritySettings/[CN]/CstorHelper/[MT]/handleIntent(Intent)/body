{
  if (intent == null)   return;
  String action=intent.getAction();
  if (Credentials.UNLOCK_ACTION.equals(action)) {
    mExternalIntent=intent;
    showCstorDialog(mState == KeyStore.UNINITIALIZED ? CSTOR_INIT_DIALOG : CSTOR_UNLOCK_DIALOG);
  }
}
