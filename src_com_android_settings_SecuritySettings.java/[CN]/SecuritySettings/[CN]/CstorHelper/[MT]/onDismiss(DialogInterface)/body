{
  if (mRetry) {
    showCstorDialog(mDialogId);
  }
 else {
    removeDialog(mDialogId);
    if (mExternalIntent != null) {
      if (Credentials.SYSTEM_INSTALL_ACTION.equals(mExternalIntent.getAction())) {
      }
 else {
        finish();
      }
    }
  }
}
