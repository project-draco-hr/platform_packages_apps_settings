{
  Settings.Secure.putString(getContentResolver(),Settings.Secure.LOCATION_PROVIDERS_ALLOWED,providers);
  if (Config.LOGV) {
    Log.v("Location Accuracy","Setting LOCATION_PROVIDERS_ALLOWED = " + providers);
  }
  mLocationManager.updateProviders();
}
