{
  mCurHeader=header;
  int index=mHeaders.indexOf(header);
  if (mDrawer != null) {
    if (index >= 0) {
      mDrawer.setItemChecked(index,true);
    }
 else {
      mDrawer.clearChoices();
    }
  }
}
