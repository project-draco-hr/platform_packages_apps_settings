{
  mIndicatorView.setImageState(mIsListExpanded ? GROUP_EXPANDED_STATE_SET : EMPTY_STATE_SET,false);
  mListView.setVisibility(mIsListExpanded ? View.VISIBLE : View.GONE);
  mContainerView.setLayoutParams(mIsListExpanded ? SHOW_LAYOUT_PARAMS : HIDE_LAYOUT_PARAMS);
}
