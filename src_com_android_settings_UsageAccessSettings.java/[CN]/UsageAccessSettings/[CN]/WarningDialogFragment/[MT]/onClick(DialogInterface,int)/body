{
  if (which == DialogInterface.BUTTON_POSITIVE) {
    ((UsageAccessSettings)getParentFragment()).allowAccess(getArguments().getString(ARG_PACKAGE_NAME),getArguments().getInt(ARG_PROFILE_ID));
  }
 else {
    dialog.cancel();
  }
}
