{
  if (preference == mRecognizerPref) {
    String setting=(String)newValue;
    Settings.Secure.putString(mFragment.getContentResolver(),Settings.Secure.VOICE_RECOGNITION_SERVICE,setting);
    updateSettingsLink(setting);
  }
  return true;
}
