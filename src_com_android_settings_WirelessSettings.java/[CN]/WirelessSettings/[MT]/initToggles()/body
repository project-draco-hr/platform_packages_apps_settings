{
  mWifiEnabler=new WifiEnabler(this,(WifiManager)getSystemService(WIFI_SERVICE),(CheckBoxPreference)findPreference(KEY_TOGGLE_WIFI));
  mAirplaneModeEnabler=new AirplaneModeEnabler(this,(CheckBoxPreference)findPreference(KEY_TOGGLE_AIRPLANE));
  mToggleBluetooth=(CheckBoxPreference)findPreference(KEY_TOGGLE_BLUETOOTH);
  mToggleBluetooth.setPersistent(false);
}
