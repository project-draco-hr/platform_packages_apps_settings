{
  if (DEBUG)   Log.d(TAG,"loadZenModeSetting " + reason);
  mMode=Settings.Global.getInt(mResolver,Settings.Global.ZEN_MODE,Settings.Global.ZEN_MODE_OFF);
  setValue(Integer.toString(mMode));
  updateConfigureVisibility();
}
