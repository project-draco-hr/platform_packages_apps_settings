{
  String currentSetting=Settings.Secure.getString(getContext().getContentResolver(),Settings.Secure.ASSISTANT);
  return currentSetting == null ? null : ComponentName.unflattenFromString(currentSetting);
}
