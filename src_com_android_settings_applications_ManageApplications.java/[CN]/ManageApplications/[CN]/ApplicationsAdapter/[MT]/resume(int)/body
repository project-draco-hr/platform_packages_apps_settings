{
  if (DEBUG)   Log.i(TAG,"Resume!  mResumed=" + mResumed);
  if (!mResumed) {
    mResumed=true;
    mSession.resume();
    mLastSortMode=sort;
    if (mNotifBridge != null) {
      mNotifBridge.resume();
    }
    rebuild(true);
  }
 else {
    rebuild(sort);
  }
}
