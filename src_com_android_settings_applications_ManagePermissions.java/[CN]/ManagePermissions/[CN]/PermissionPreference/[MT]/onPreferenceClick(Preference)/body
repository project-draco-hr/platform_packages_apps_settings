{
  Intent i=new Intent(Intent.ACTION_MANAGE_PERMISSION_APPS).putExtra(Intent.EXTRA_PERMISSION_NAME,mGroup.name);
  try {
    getActivity().startActivity(i);
  }
 catch (  ActivityNotFoundException e) {
    Log.w(TAG,"No app to handle " + i.getAction());
  }
  return true;
}
