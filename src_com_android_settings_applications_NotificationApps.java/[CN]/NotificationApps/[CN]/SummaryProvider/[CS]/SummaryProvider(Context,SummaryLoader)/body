{
  mContext=context;
  mLoader=loader;
  mAppState=ApplicationsState.getInstance((Application)context.getApplicationContext());
  mNotifBackend=new NotificationBackend();
  mHandler=new Handler(mAppState.getBackgroundLooper());
}
