{
  if (DEBUG)   Log.d(TAG,"onPositive");
  savePermissionChoice(mRequestType,CachedBluetoothDevice.ACCESS_ALLOWED);
  sendIntentToReceiver(BluetoothDevice.ACTION_CONNECTION_ACCESS_REPLY,true,BluetoothDevice.EXTRA_ALWAYS_ALLOWED,true);
  finish();
}
