{
  mOnlineModePreference.setChecked(mOnlineMode);
  mOnlineModePreference.setEnabled(!mDevice.isBusy());
  mOnlineModePreference.setSummary(mOnlineMode ? mDevice.getSummary() : R.string.bluetooth_device_advanced_online_mode_summary);
}
