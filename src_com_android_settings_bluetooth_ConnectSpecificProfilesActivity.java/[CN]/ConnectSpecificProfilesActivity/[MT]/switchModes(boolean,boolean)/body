{
  if (mOnlineMode != onlineMode || force) {
    mOnlineMode=onlineMode;
    if (onlineMode) {
      mDevice.connect();
    }
 else {
      mDevice.disconnect();
    }
    refreshOnlineModePreference();
  }
}
