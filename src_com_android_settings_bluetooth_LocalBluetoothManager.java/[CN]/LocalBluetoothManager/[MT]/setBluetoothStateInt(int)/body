{
  mState=state;
  if (state == BluetoothAdapter.BLUETOOTH_STATE_ON || state == BluetoothAdapter.BLUETOOTH_STATE_OFF) {
    mCachedDeviceManager.onBluetoothStateChanged(state == BluetoothAdapter.BLUETOOTH_STATE_ON);
  }
}
