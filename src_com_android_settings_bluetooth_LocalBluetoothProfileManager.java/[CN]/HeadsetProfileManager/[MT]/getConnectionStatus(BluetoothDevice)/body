{
  BluetoothDevice currentDevice=mService.getCurrentHeadset();
  return currentDevice != null && currentDevice.equals(device) ? convertState(mService.getState(device)) : SettingsBtStatus.CONNECTION_STATUS_DISCONNECTED;
}
