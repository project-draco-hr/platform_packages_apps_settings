{
  mUiHandler.post(new Runnable(){
    public void run(){
      String address=mService.getHeadsetAddress();
      if (TextUtils.isEmpty(address))       return;
      mLocalManager.getLocalDeviceManager().onProfileStateChanged(address,Profile.HEADSET,BluetoothHeadset.STATE_CONNECTED);
    }
  }
);
}
