{
  super.onCreate(savedInstanceState);
  mUserHandle=Utils.getProfileToDisplay(ActivityManagerNative.getDefault(),getActivity().getActivityToken(),savedInstanceState);
  addPreferencesFromResource(R.xml.bluetooth_message_access);
}
