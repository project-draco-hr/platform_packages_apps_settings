{
  if (pref != null) {
    String title=pref.getTitle().toString();
    if (isMultiSIM) {
      title+=" " + getResources().getString(R.string.slot_number,slotId + 1);
    }
    pref.setTitle(title);
  }
}
