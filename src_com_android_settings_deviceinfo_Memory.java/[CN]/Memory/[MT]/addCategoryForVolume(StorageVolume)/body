{
  final StorageVolumePreferenceCategory category=new StorageVolumePreferenceCategory(getActivity(),volume,mStorageManager);
  mCategories.add(category);
  getPreferenceScreen().addPreference(category);
  category.init();
}
