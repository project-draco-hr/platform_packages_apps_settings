{
  try {
    if (mFormatPrivate) {
      mStorage.partitionPrivate(mDisk.getId());
    }
 else {
      mStorage.partitionPublic(mDisk.getId());
    }
    return null;
  }
 catch (  Exception e) {
    return e;
  }
}
