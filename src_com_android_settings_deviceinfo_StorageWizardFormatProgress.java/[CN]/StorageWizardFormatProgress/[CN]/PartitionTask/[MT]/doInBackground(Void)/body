{
  try {
    if (mFormatPublic) {
      mStorage.partitionPublic(mDisk.id);
    }
 else {
      mStorage.partitionPrivate(mDisk.id);
    }
    return null;
  }
 catch (  Exception e) {
    return e;
  }
}
