{
  mValues=values;
  mLabel=label;
  if (iconId > 0) {
    mIcon=getResources().getDrawable(iconId);
  }
  if (mValues != null)   mValue=mValues[0];
  if ((label == null || iconId == 0) && uid > 0) {
    getNameForUid(uid);
  }
}
