{
  if (currentSci == null) {
    setValue(null);
    return;
  }
  for (int i=0; i < mScis.length; i++) {
    if (mScis[i].getId().equals(currentSci.getId())) {
      setValueIndex(i);
      return;
    }
  }
}
