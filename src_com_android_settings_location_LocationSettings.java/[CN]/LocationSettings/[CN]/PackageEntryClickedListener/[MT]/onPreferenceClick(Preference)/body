{
  Bundle args=new Bundle();
  args.putString(InstalledAppDetails.ARG_PACKAGE_NAME,mPackage);
  ((SettingsActivity)getActivity()).startPreferencePanelAsUser(InstalledAppDetails.class.getName(),args,R.string.application_info_label,null,mUserHandle);
  return true;
}
