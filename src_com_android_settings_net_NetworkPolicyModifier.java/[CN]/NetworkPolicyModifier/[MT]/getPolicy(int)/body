{
  for (  NetworkPolicy policy : mPolicies) {
    if (policy.networkTemplate == networkTemplate && Objects.equal(policy.subscriberId,mSubscriberId)) {
      return policy;
    }
  }
  return null;
}
