{
  super.onBindViewHolder(view);
  if (mSystemApp) {
    ((ImageView)view.findViewById(R.id.low_importance)).getDrawable().setTint(getContext().getColor(R.color.importance_disabled_tint));
  }
  view.setDividerAllowedAbove(false);
  view.setDividerAllowedBelow(false);
}
