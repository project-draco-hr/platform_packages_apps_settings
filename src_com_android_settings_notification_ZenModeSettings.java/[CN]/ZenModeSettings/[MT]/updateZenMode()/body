{
  final boolean zenMode=Global.getInt(getContentResolver(),Global.ZEN_MODE,Global.ZEN_MODE_OFF) != Global.ZEN_MODE_OFF;
  if (mSwitchBar.isSwitchChecked() != zenMode) {
    mSwitchBar.setSwitchChecked(zenMode);
    mIgnoreNext=true;
  }
}
