{
  ToggleSwitch toggleSwitch=new ToggleSwitch(context);
  final int padding=context.getResources().getDimensionPixelSize(R.dimen.action_bar_switch_padding);
  toggleSwitch.setPaddingRelative(0,0,padding,0);
  return toggleSwitch;
}
