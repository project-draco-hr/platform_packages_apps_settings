{
  if (oldVersion == 100 || oldVersion == 101 || oldVersion == 102) {
    Log.w(TAG,"Detected schema version 100, 101 or 102. " + "Index needs to be rebuilt for schema version 103");
    dropTables(db);
    bootstrapDB(db);
  }
}
