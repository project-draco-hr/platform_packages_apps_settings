{
  if (mEditUserInfoDialog != null && mEditUserInfoDialog.isShowing() && mEditUserPhotoController != null) {
    outState.putParcelable(RestrictedProfileSettings.KEY_SAVED_PHOTO,mEditUserPhotoController.getNewUserPhotoBitmap());
  }
  if (mWaitingForActivityResult) {
    outState.putBoolean(RestrictedProfileSettings.KEY_AWAITING_RESULT,mWaitingForActivityResult);
  }
}
