{
  final File folder=context.getCacheDir();
  folder.mkdirs();
  final File fullPath=new File(folder,fileName);
  fullPath.delete();
  final Uri fileUri=FileProvider.getUriForFile(context,FILE_PROVIDER_AUTHORITY,fullPath);
  return fileUri;
}
