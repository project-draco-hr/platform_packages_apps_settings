{
  UserInfo newUserInfo=mUserManager.createUser(getResources().getString(R.string.user_new_user_name),0);
  if (newUserInfo != null) {
    assignDefaultPhoto(newUserInfo);
  }
  return newUserInfo;
}
