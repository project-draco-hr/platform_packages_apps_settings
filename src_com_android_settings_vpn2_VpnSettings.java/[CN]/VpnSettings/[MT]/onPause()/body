{
  super.onPause();
  if (mUnavailable) {
    return;
  }
  if (mDialog != null) {
    mDialog.setOnDismissListener(null);
    mDialog.dismiss();
  }
  if (getView() != null) {
    unregisterForContextMenu(getListView());
  }
}
