{
  final ChartSweepView dynamic=mValidBeforeDynamic;
  final boolean dynamicEnabled=dynamic != null && dynamic.isEnabled();
  return Math.min(mValidBefore,dynamicEnabled ? dynamic.getValue() - mValidBufferArea : Long.MAX_VALUE);
}
