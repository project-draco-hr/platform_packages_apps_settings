{
  int posBase=Math.max(0,((int)x - LabeledSeekBar.this.getPaddingStart()) / getHalfVirtualViewWidth());
  posBase=(posBase + 1) / 2;
  return mIsLayoutRtl ? LabeledSeekBar.this.getMax() - posBase : posBase;
}
