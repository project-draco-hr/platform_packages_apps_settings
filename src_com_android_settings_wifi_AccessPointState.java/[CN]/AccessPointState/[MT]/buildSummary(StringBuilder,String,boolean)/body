{
  if (sb.length() == 0) {
    sb.append(string);
  }
 else {
    sb.append(", ");
    if (autoLowerCaseFirstLetter) {
      sb.append(Character.toLowerCase(string.charAt(0))).append(string,1,string.length());
    }
 else {
      sb.append(string);
    }
  }
}
