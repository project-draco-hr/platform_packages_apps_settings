{
  if (mWifiManager.isWifiEnabled()) {
    String summary=WifiStatus.getStatus(mContext,mWifiManager.getConnectionInfo().getSSID(),networkInfo.getDetailedState());
    mCheckBox.setSummary(summary);
  }
}
