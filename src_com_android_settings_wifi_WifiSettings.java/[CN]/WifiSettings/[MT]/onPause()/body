{
  super.onPause();
  unregisterReceiver(mReceiver);
  if (mWifiEnabler != null) {
    mWifiEnabler.pause();
  }
  mScanner.pause();
  if (mDialog != null) {
    mDialog.dismiss();
    mDialog=null;
  }
  if (mResetNetworks) {
    enableNetworks();
  }
}
